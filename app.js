// types of export 
// 1. named export => adding export infront of variable name and during import you need to write correct variable name inside curly brases
    //  export let b = 10  
    // import {b} from "./app.js" 
// 2. default export => used to access only one value and during import you can give any variable name to import it. No need to give name as it is.
//      let a = 20
//      export default a 
//      import b from "./app.js"

// Real DOM & Virtual DOM
// Real DOM => which used to work with HTML
// Virtual DOM => 